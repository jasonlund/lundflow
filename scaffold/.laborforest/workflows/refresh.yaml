resource_type: workflow
require_status: ready
ending_status: ready
sort_order: 1
steps:
  # Destructive by design — this is the reseed. `require_status: ready` and the
  # primary checkout is ready, so `lf run refresh` can be invoked there directly:
  # up's guard on its nested call does not travel with this file, and every step
  # below therefore carries its own. Run against the primary it would drop the
  # primary's own database, whose rows nothing a worktree seeds from can restore.
  #
  # A project that loads a worktree's data from more than `db:seed` (a committed
  # dump, an import command) adds that step here, guarded the same way.
  - name: 'Rebuild the schema'
    type: shell
    if: 'test "{{ WORKSPACE_DIR }}" != "{{ PROJECT_PRIMARY_DIR }}"'
    run: 'php artisan migrate:fresh --force'
  - name: 'Seed the application data'
    type: shell
    if: 'test "{{ WORKSPACE_DIR }}" != "{{ PROJECT_PRIMARY_DIR }}"'
    run: 'php artisan db:seed --force'
