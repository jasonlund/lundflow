resource_type: workflow
require_status: ready
ending_status: suspended
sort_order: 100
steps:
  # `up` creates nothing outside the worktree, so there is nothing to drop or
  # unlink; removing the worktree itself is LaborForest's GUI action.
  - name: 'Nothing to tear down'
    type: shell
    run: 'true'
